<Application x:Class="ExamenRVS.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:ExamenRVS"
             xmlns:views="clr-namespace:ExamenRVS.Views"
             xmlns:viewmodels="clr-namespace:ExamenRVS.ViewModels"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <ResourceDictionary>
            <DataTemplate DataType="{x:Type viewmodels:HomeViewModel}">
                <views:HomeView/>
            </DataTemplate>
            
            <DataTemplate DataType="{x:Type viewmodels:ResumenViewModel}">
                <views:ResumenView/>
            </DataTemplate>
        </ResourceDictionary>
        
    </Application.Resources>
</Application>
